<form method="post" action="{{ path('app_activity_delete', {'id': activity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activity.id) }}">
    <button class="btn">Delete</button>
</form>
<style>
button.btn {
    background-color: #ff4d4d; /* Couleur de fond rouge */
    color: white; /* Couleur du texte */
    border: none; /* Aucune bordure */
    cursor: pointer; /* Curseur en forme de main */
    padding: 10px 20px; /* Espacement intérieur */
    border-radius: 5px; /* Bordures arrondies */
    font-weight: bold; /* Texte en gras */
    transition: background-color 0.3s ease, box-shadow 0.3s ease; /* Transition douce pour le fond et l'ombre */
    outline: none; /* Supprime le contour lors du focus */
}

/* Effet au survol du bouton */
button.btn:hover {
    background-color: #e60000; /* Couleur de fond plus foncée au survol */
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2); /* Ombre douce pour un effet 3D léger */
}

/* Style optionnel pour le bouton lorsqu'il est pressé (active) */
button.btn:active {
    box-shadow: inset 0 2px 5px rgba(0, 0, 0, 0.3); /* Ombre intérieure pour un effet enfoncé */
}
</style>