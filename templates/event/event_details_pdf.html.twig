<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Event Details - {{ event.titre }}</title>
    <style>
        @font-face {
            font-family: 'Open Sans', sans-serif;
            font-style: normal;
            font-weight: normal;
            src: url('https://fonts.googleapis.com/css2?family=Open+Sans&display=swap') format('truetype');
        }
        body {
            font-family: 'Open Sans', sans-serif;
        }
        .container {
            padding: 20px;
        }
        .header {
            background-color: #f0f0f0;
            padding: 10px;
            text-align: center;
            margin-bottom: 20px;
        }
        h1 {
            color: #333;
        }
        .event-detail, .activities {
            margin-bottom: 15px;
        }
        .activity {
            margin-left: 20px;
            margin-top: 5px;
        }
        .activity-header {
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Event Details - {{ event.titre }}</h1>
            <p>Date: {{ event.date|date('Y-m-d H:i') }}</p>
            <p>Location: {{ event.lieu }}</p>
        </div>
        <div class="activities">
            <strong>Activities:</strong>
            {% if event.activities|length > 0 %}
                {% for activity in event.activities %}
                    <div class="activity">
                        <span class="activity-header">{{ activity.nom }}</span><br>
                        <strong>Status:</strong> {{ activity.status }}<br>
                        <strong>Description:</strong> {{ activity.description }}<br>
                        <strong>Organizer:</strong> {{ activity.organisateur }}
                    </div>
                {% endfor %}
            {% else %}
                <p>No activities listed for this event.</p>
            {% endif %}
        </div>
    </div>
</body>
</html>
