


{% block title %}Liste des Produit{% endblock %}

{% block body %}
    <!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Liste des Produit</title>
        <link rel="stylesheet" href="front/styles.css" /> <!-- Assurez-vous d'inclure vos styles CSS ici -->

        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
        <style>
            /* Style pour la mise en forme */
            .product-container {
                display: flex;
                flex-wrap: wrap;
                gap: 20px;
            }
            .product {
                width: calc(25% - 20px);
                border: 1px solid #ccc;
                padding: 10px;
                border-radius: 5px;
            }
            .product img {
                max-width: 100%;
                height: auto;
            }

            /* Style pour le background */
            body {
                background-image: url('{{ asset("uploads/copy-space-stethoscope-medicine_23-2148551056.jpg") }}');
                background-size: cover;
                background-position: center;
                min-height: 100vh; /* Pour assurer que l'image de fond couvre toute la hauteur de la fenêtre */
                margin: 0; /* Supprime les marges par défaut du corps */
                padding: 0; /* Supprime les rembourrages par défaut du corps */
                font-family: Arial, sans-serif; /* Définir une police de caractères par défaut */
            }
        </style>
    </head>
    <body>
        <div class="container">
            <nav>
                <div class="nav__logo">HealthCare</div>
                <ul class="nav__links">
                    <li class="link"><a href="#">Home</a></li>
                    <li class="link"><a href="Affichef">Produit</a></li>
                    <!-- ******************************************Categorie BOUTON***********************************************************-->
                    <style>
                        .OCategorie-toggle {
                            background-color: transparent;
                            border: none; /* Remove border */
                            color: grey; /* Set text color to white */
                        }

                        .OCategorie-toggle:hover {
                            background-color: transparent;
                            color: grey; /* Set text color to white on hover */
                        }

                        .Categorie-menu {
                            display: none;
                            position: absolute;
                            background-color: #f9f9f9;
                            min-width: 160px;
                            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
                            z-index: 1;
                        }

                        .Categorie:hover .Categorie-menu {
                            display: block;
                        }

                        .Categorie-menu a {
                            color: black;
                            padding: 12px 16px;
                            text-decoration: none;
                            display: block;
                        }

                        .Categorie-menu a:hover {
                            background-color: #f1f1f1;
                        }
                    </style>

                    <div class="Categorie">
                        <button class="btn btn-secondary OCategorie-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Categorie
                        </button>
                        <div class="Categorie-menu dropdown-menu">
                            <a class="dropdown-item" href="Addc">Ajouter</a>
                            <a class="dropdown-item" href="Affichec">Afficher</a>
                        </div>
                    </div>

                    <li class="link"><a href="#">About Us</a></li>
                    <li class="link"><a href="#">Courses</a></li>
                    <li class="link"><a href="#">Pages</a></li>
                    <li class="link"><a href="#">Blog</a></li>
                    <li class="link"><a href="#">Contact</a></li>
                </ul>
                    
            </nav>

            <style>
                body {
                    font-family: Arial, sans-serif;le
                    background-color: #f8f9fa;
                }

                .container {
                    margin-top: 50px;
                }

                h1 {
                    text-align: center;
                    margin-bottom: 30px;
                }

                .product-container {
                    display: flex;
                    flex-wrap: wrap;
                    justify-content: center;
                }

                .product {
                    background-color: #fff;
                    border-radius: 10px;
                    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
                    margin: 20px;
                    padding: 20px;
                    max-width: 300px;
                }

                .product img {
                    max-width: 100%;
                    height: auto;
                    border-radius: 5px;
                }

                .product h2 {
                    margin-top: 20px;
                    margin-bottom: 10px;
                    font-size: 20px;
                }

                .product p {
                    color: #6c757d;
                }

                .product .price {
                    font-weight: bold;
                    font-size: 18px;
                    color: #dc3545;
                    margin-top: 10px;
                    text-align: center;
                }
            </style>
 
<h1>Liste des Produits</h1>

<form action="{{ path('searchproduit') }}" method="GET" class="form-inline">
    <div class="input-group">
        <input type="text" name="keyword" required="required" pattern=".{2,}" class="form-control" placeholder="Chercher le produit...">
        <button class="btn btn-warning" type="submit">
            <i class="fa fa-search"></i> Rechercher
        </button>
    </div>
</form>

<a href="{{ path('app_affichef') }}" class="btn btn-success mt-3">
    <i class="fa fa-backward"></i> Retour
</a>

   </form>

<div class="form-group">
<a href="{{ path('list_produit') }}" class="btn btn-danger tooltips" data-placement="top" data-toggle="tooltip" data-original-title="Detail">
    <i class="fa fa-file-pdf-o" aria-hidden="true"></i> PDF
</a>
<div class="product-container">
    {% for p in produit %}
        <div class="product">
            <img src="{{ asset(p.img) }}" alt="{{ p.nom }}">
            <h2>{{ p.nom }}</h2>
            <p>{{ p.description }}</p>
            <p class="price">{{ p.prix }} TND</p>
            <button class="btn btn-primary" onclick="handlePayment('{{ p.id }}', '{{ p.nom }}', {{ p.prix }})">Payer</button>
            
        </div>
        <script>
    function handlePayment(produitId, nom, prix) {
        // Vous pouvez utiliser JavaScript pour envoyer une requête AJAX
        // pour déclencher le paiement ou simplement rediriger vers une route Symfony
        // qui gère le processus de paiement côté serveur.
        // Dans cet exemple, je vais simplement rediriger vers une route Symfony.

        window.location.href = "{{ path('checkout') }}" + "?productId=" + produitId + "&nom=" + nom+ "&prix=" + prix;
    }
</script>
    {% endfor %}
    <nav aria-label="Page navigation example">
    <ul class="pagination">
       {% if produit is defined and attribute(produit, 'currentPageNumber') is defined %}
    {% if produit.currentPageNumber > 1 %}
        <li class="page-item">
            <a class="page-link" href="{{ path('app_affichef', {'page': produit.currentPageNumber - 1}) }}" aria-label="Previous">
                <span aria-hidden="true">Previous</span>
            </a>
        </li>
    {% endif %}

    {% for page in range(1, produit.pageCount) %}
        <li class="page-item{% if produit.currentPageNumber == page %} active{% endif %}">
            <a class="page-link" href="{{ path('app_affichef', {'page': page}) }}">{{ page }}</a>
        </li>
    {% endfor %}

    {% if produit.currentPageNumber < produit.pageCount %}
        <li class="page-item">
            <a class="page-link" href="{{ path('app_affichef', {'page': produit.currentPageNumber + 1}) }}" aria-label="Next">
                <span aria-hidden="true">Next</span>
            </a>
        </li>
    {% endif %}
{% endif %}

    </ul>
</nav>

</div>
        </script>
        <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    </body>
    </html>
{% endblock %}
