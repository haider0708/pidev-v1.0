{#<!DOCTYPE html>

<title>Hello RatingController!</title>

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Users/DRISS Manel/PIDEV/src/Controller/RatingController.php'|file_link(0) }}">src/Controller/RatingController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/DRISS Manel/PIDEV/templates/rating/index.html.twig'|file_link(0) }}">templates/rating/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}#}
{# templates/rating/index.html.twig #}

{# templates/rating/index.html.twig #}
{% extends 'front/index.html.twig' %}

{% block body %}
    {% for pharmacie in pharmacies %}
        <div class="rating">
            {% set note = pharmacie.note ?? 0 %}
            {% for i in range(1, 5) %}
                {% set checked = i <= note ? 'checked' : '' %}
                <input type="radio" id="star{{ i }}" name="rating" value="{{ i }}" style="display: none;"  {{ checked }}>
                <label for="star{{ i }}">&#9733;</label>
            {% endfor %}
        </div>
        <p id="selectedRating">Note de la pharmacie : <span id="ratingValue">{{ note }}</span></p>
    {% endfor %}

    <script src="{{ asset('js/rating.js') }}"></script>
{% endblock %}
